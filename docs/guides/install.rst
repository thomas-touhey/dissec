.. _guide-install:

Installing dissec
=================

In order to install dissec, the instructions may depend on the system you
want to install it on.

|pypi| pip (generic)
--------------------

dissec can be installed via ``pip``::

    pip install dissec

.. |pypi| image:: pypi.png
